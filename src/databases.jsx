import { useState } from 'preact/hooks'
import './page-styles.css'
import { PageFrame, PageSection, Faq } from './pages.jsx'
import { CodeBlock } from './code.jsx'

function CopyCommand({ command }) {
  const [copied, setCopied] = useState(false)
  const copy = () => {
    const done = () => { setCopied(true); window.setTimeout(() => setCopied(false), 2000) }
    if (navigator.clipboard?.writeText) {
      navigator.clipboard.writeText(command).then(done).catch(() => {
        const el = document.createElement('textarea')
        el.value = command
        el.style.position = 'fixed'
        el.style.opacity = '0'
        document.body.appendChild(el)
        el.select()
        document.execCommand('copy')
        document.body.removeChild(el)
        done()
      })
    } else { done() }
  }
  return <button className="install-copy" type="button" onClick={copy} title="Copy install command" aria-label="Copy install command"><i>$</i> {command}{copied ? <span className="copy-pop">copied</span> : null}</button>
}

const extras = [
  { command: 'uv add "dbwarden[postgres]"', what: 'PostgreSQL driver (psycopg2-binary). The one most projects need; PostgreSQL is the primary backend.' },
  { command: 'uv add "dbwarden[mysql]"', what: 'MySQL and MariaDB driver (pymysql). One extra covers both, since the two share the MySQL wire protocol.' },
  { command: 'uv add "dbwarden[clickhouse]"', what: 'ClickHouse driver (clickhouse-connect) plus aiohttp for the HTTP transport.' },
  { command: 'uv add "dbwarden[sqlite]"', what: 'SQLite needs no driver; it ships inside the Python standard library. The extra exists for symmetry and is empty.' },
]

export function DatabasesPage({ dark, toggleTheme }) {
  return <PageFrame dark={dark} toggleTheme={toggleTheme} eyebrow="databases" title={<>Database Support.</>} intro={<>All five backends follow the same model-driven loop: models define the schema, `make-migrations` generates the SQL, and `migrate` applies it. The strongest combination for most projects: <strong>SQLAlchemy + PostgreSQL + FastAPI</strong>. Backend-specific options are declared in typed metadata on the models, and the matrix below lists what each backend supports.</>} install="uv add dbwarden">
    <div className="definition-note"><strong>Round-trip, defined</strong><p>Round-trip means dbwarden can read a schema and write it back. <code className="inline-code">generate-models</code> reads a live database and emits SQLAlchemy models; <code className="inline-code">make-migrations</code> and <code className="inline-code">migrate</code> write models back to SQL. Backends with round-trip support cover both directions. Backends without it are still migration targets, but introspection back into models is not yet supported.</p></div>
    <PageSection number="01" label="The matrix" title="Round-trip support, per backend." doc="https://docs.dbwarden.org/databases/"><div className="databases-table-wrap"><table className="databases-table"><thead><tr><th>Backend</th><th>database_type</th><th>Round-trip</th><th>Notes</th></tr></thead><tbody><tr><th>PostgreSQL</th><td><code>postgresql</code></td><td>Yes</td><td>Primary backend, full schema fidelity</td></tr><tr><th>MySQL</th><td><code>mysql</code></td><td>Yes</td><td>DDL parity focus</td></tr><tr><th>ClickHouse</th><td><code>clickhouse</code></td><td>Yes</td><td>MergeTree engine family</td></tr><tr><th>MariaDB</th><td><code>mariadb</code></td><td>No</td><td>Migration target; introspection gaps remain</td></tr><tr><th>SQLite</th><td><code>sqlite</code></td><td>Yes</td><td>Full round-trip; also the <code>dev-mode</code> backend</td></tr></tbody></table></div></PageSection>
    <PageSection number="02" label="PostgreSQL" title="The primary backend, with full round-trip." doc="https://docs.dbwarden.org/databases/postgresql/">
      <div className="why-split"><div><p>PostgreSQL is where the snapshot, diff, and SQL emission pipeline is deepest. Identity columns (<code className="inline-code">GENERATED ALWAYS</code> and <code className="inline-code">BY DEFAULT AS IDENTITY</code>, with sequence options), generated stored columns, partitioning (<code className="inline-code">RANGE</code> / <code className="inline-code">LIST</code> / <code className="inline-code">HASH</code> with attach and detach), table inheritance, exclusion constraints, deferrable constraints, and advanced indexes all round-trip. Indexes keep their partial and expression predicates, <code className="inline-code">INCLUDE</code> columns, operator classes, <code className="inline-code">NULLS NOT DISTINCT</code>, column sort order, and <code className="inline-code">CONCURRENTLY</code>.</p><p>Per-column <code className="inline-code">COLLATE</code>, <code className="inline-code">STORAGE</code>, and <code className="inline-code">COMPRESSION</code> (<code className="inline-code">pglz</code>, <code className="inline-code">zstd</code>, PG 14+) survive the trip, as do enums, domains, composite types, sequences, functions, triggers, roles, row-level security policies, extended statistics, event triggers, views and materialized views, and schema and table grants. Default privileges (<code className="inline-code">ALTER DEFAULT PRIVILEGES</code>) are handled for future objects, so the access story covers new tables created after the migration, not just the ones that exist today.</p><p>Round-trip is tested rather than assumed: <code className="inline-code">generate-models</code> reverse-engineers a live database, and feeding that output back through <code className="inline-code">make-migrations</code> has to produce zero diff. Everything below the SQLAlchemy type is carried in typed metadata on the model: table-level options on <code className="inline-code">Meta(PGTableMeta)</code>, column-level options on a <code className="inline-code">Meta</code> class per column with <code className="inline-code">pg.field()</code> specs.</p></div><div><span className="comparison-label">typed metadata on the model</span><CodeBlock>{`from dbwarden.databases.pgsql import PGTableMeta, PGColumnMeta, pg\n\nclass User(Base):\n    __tablename__ = "users"\n\n    id: Mapped[int] = mapped_column(Integer, primary_key=True)\n\n    class Meta(PGTableMeta):\n        pg_fillfactor = 80\n        pg_schema = "app"\n\n        class id(PGColumnMeta):\n            pg = pg.field(identity="always")`}</CodeBlock><span className="comparison-label">install the driver</span><CopyCommand command='uv add "dbwarden[postgres]"' /></div></div>
    </PageSection>
    <PageSection number="03" label="MySQL and MariaDB" title="Full round-trip for MySQL. Target-only for MariaDB." doc="https://docs.dbwarden.org/databases/mysql/">
      <div className="why-split"><div><p>MySQL round-trip covers the storage engine (<code className="inline-code">my_engine</code>), table and per-column charsets and collations, row formats, unsigned integer columns, <code className="inline-code">ON UPDATE CURRENT_TIMESTAMP</code>, and the auto-increment lifecycle, including toggling <code className="inline-code">AUTO_INCREMENT</code> on an integer primary key, which emits a <code className="inline-code">MODIFY COLUMN</code>. Types are normalized: <code className="inline-code">TINYINT(1)</code> becomes <code className="inline-code">BOOLEAN</code>, and the rest map to their SQLAlchemy equivalents. Foreign keys keep their <code className="inline-code">ON DELETE</code> / <code className="inline-code">ON UPDATE</code> options, and dropping one emits MySQL's <code className="inline-code">DROP FOREIGN KEY</code> syntax.</p><p>One thing to know before relying on it: MySQL DDL is non-transactional. Each statement auto-commits, so a migration that fails halfway leaves the earlier statements applied. Review generated files in order, and treat partial failure as possible in your recovery plan.</p><p>MariaDB is a supported migration target with its own typed metadata, including MariaDB-specific features: page compression (<code className="inline-code">mdb_page_compressed</code>, <code className="inline-code">mdb_page_compression_level</code>), invisible columns, and <code className="inline-code">CREATE SEQUENCE</code>. Schema introspection back into models is not yet round-trip, so MariaDB appears in the matrix as target-only.</p></div><div><span className="comparison-label">engine and charset on the model</span><CodeBlock>{`from dbwarden.databases.mysql import MyTableMeta, MyColumnMeta, my\n\nclass User(Base):\n    __tablename__ = "users"\n\n    id: Mapped[int] = mapped_column(Integer, primary_key=True)\n\n    class Meta(MyTableMeta):\n        my_engine = "InnoDB"\n        my_charset = "utf8mb4"\n\n        class id(MyColumnMeta):\n            my = my.field(unsigned=True)`}</CodeBlock><span className="comparison-label">install the driver</span><CopyCommand command='uv add "dbwarden[mysql]"' /></div></div>
    </PageSection>
    <PageSection number="04" label="ClickHouse" title="Analytical storage in the same workflow." doc="https://docs.dbwarden.org/databases/clickhouse/">
      <div className="why-split"><div><p>ClickHouse's object model is different enough to matter: what is a <code className="inline-code">SET</code> in PostgreSQL is often a <code className="inline-code">CREATE</code> commitment in ClickHouse. The docs put it directly: some things can never change, and some changes force a table recreate. dbwarden models that line explicitly instead of pretending every diff is a gentle <code className="inline-code">ALTER</code>.</p><p>Engines, projections, skip indexes, dictionaries, materialized views, and column codecs all live in typed metadata. The <code className="inline-code">MergeTree</code> family is configured through engine factories and settings, with <code className="inline-code">order_by</code>, <code className="inline-code">partition_by</code>, and <code className="inline-code">ch_indexes</code> declared next to the table. <code className="inline-code">LowCardinality</code> and <code className="inline-code">Nullable</code> wrappers are part of the type system, and data operations like <code className="inline-code">OPTIMIZE</code> and <code className="inline-code">MATERIALIZE</code> have their own declared surface.</p><p>Because recreates are the cost of changing an engine, the policy is explicit: engine changes go through a recreate pipeline, and transitions between engine families that would lose data are refused as rollback rather than silently claimed.</p></div><div><span className="comparison-label">engine, order by, and indexes</span><CodeBlock>{`from dbwarden.databases.clickhouse import CHTableMeta, ChEngineSpec, ChIndexSpec\n\nclass Event(Base):\n    __tablename__ = "events"\n\n    class Meta(CHTableMeta):\n        ch_engine = ChEngineSpec("MergeTree")\n        ch_order_by = ["event_date", "id"]\n        ch_indexes = [\n            ChIndexSpec("ix_payload", ["payload"], type="bloom_filter", granularity=1),\n        ]`}</CodeBlock><span className="comparison-label">install the driver</span><CopyCommand command='uv add "dbwarden[clickhouse]"' /></div></div>
    </PageSection>
    <PageSection number="05" label="SQLite" title={<>Full round-trip, and the <code className="inline-code">dev-mode</code> backend.</>} doc="https://docs.dbwarden.org/databases/">
      <div className="why-split"><div><p>SQLite is a full round-trip backend: <code className="inline-code">generate-models</code> reads a live SQLite database back into SQLAlchemy models, and <code className="inline-code">make-migrations</code> / <code className="inline-code">migrate</code> write models back to SQLite DDL. STRICT tables, WITHOUT ROWID tables, generated columns (<code className="inline-code">sq.field(generated=...)</code> with STORED or VIRTUAL mode), and column collations all survive the trip, carried in <code className="inline-code">SqTableMeta</code> / <code className="inline-code">SqColumnMeta</code> on the model. That makes SQLite usable as a real target, not just a translation layer.</p><p>SQLite's ALTER TABLE covers only RENAME TO, RENAME COLUMN, ADD COLUMN, and DROP COLUMN. Everything else, a column's type or default, a table constraint, toggling STRICT or WITHOUT ROWID, is emitted as a table rebuild: create the new shape under a temporary name, copy every row, swap the names. The rebuild is visible in the generated SQL, and <code className="inline-code">check</code> reports it with the cost spelled out, so a migration that needs one is reviewed as a rebuild instead of left as a comment telling you to write it by hand.</p><p>Dev mode is where SQLite earns its keep: two URLs on the same database object, the production URL by default and a dev URL whenever you pass <code className="inline-code">--dev</code>. The usual pair is PostgreSQL in production and SQLite locally, which removes the server dependency from everyday work: no Docker setup, no shared state, a file you can delete to reset, and no way to accidentally touch a production-like environment.</p><p>When the dev backend can't represent a production type, dbwarden translates: UUID to TEXT, JSON/JSONB to TEXT, BYTEA to BLOB, TIMESTAMPTZ to DATETIME, SERIAL to INTEGER. Unsupported defaults are dropped with a warning, or generation fails under <code className="inline-code">--strict-translation</code>, which is the mode you want in CI so a type that production supports never silently disappears from the dev database.</p><p>The recommended rhythm: iterate locally with <code className="inline-code">--dev</code>, keep strict checks in CI with <code className="inline-code">--strict-translation</code>, and validate release-candidate migrations against a production-like database before deploy. Because SQLite needs no driver, the extra is empty and nothing extra is installed.</p></div><div><span className="comparison-label">sqlite metadata on the model</span><CodeBlock>{`from dbwarden.databases.sqlite import SqTableMeta, SqColumnMeta, sq\n\nclass User(Base):\n    __tablename__ = "users"\n\n    id: Mapped[int] = mapped_column(Integer, primary_key=True)\n    slug: Mapped[str] = mapped_column(String)\n\n    class Meta(SqTableMeta):\n        sq_without_rowid = True\n        sq_strict = True\n\n        class slug(SqColumnMeta):\n            sq = sq.field(collate="NOCASE")`}</CodeBlock><span className="comparison-label">two URLs, one database</span><CodeBlock>{`class Primary(DbwardenDatabase):\n    database_name = "primary"\n    default = True\n    database_type = "postgresql"\n    database_url_sync = "postgresql://user:pass@localhost:5432/main"\n    dev_database_type = "sqlite"\n    dev_database_url = "sqlite:///./development.db"`}</CodeBlock></div></div>
    </PageSection>
    <PageSection number="06" label="Multi-database" title="One repository, several databases, isolated histories." doc="https://docs.dbwarden.org/configuration/multi-database/">
      <div className="why-split"><div><p>Multiple databases are the normal case once a service grows: a read/write split with a replica, domain separation between transactional and analytical storage, a new database adopted while a legacy one still serves traffic, or one database per tenant. Each database declared as a <code className="inline-code">DbwardenDatabase</code> subclass gets its own migration directory, its own model set, and its own linear versioned history, so <code className="inline-code">analytics__0001_...</code> never collides with <code className="inline-code">primary__0001_...</code>.</p><p>When databases share model paths, <code className="inline-code">model_tables</code> assigns table ownership so each table belongs to exactly one database. The database marked <code className="inline-code">default = True</code> is the one used when <code className="inline-code">--database</code> is omitted. Operate one at a time with <code className="inline-code">--database</code>, or all of them together with <code className="inline-code">--all</code> for <code className="inline-code">migrate</code>, <code className="inline-code">status</code>, and <code className="inline-code">rollback</code>.</p><p><code className="inline-code">skip_if_missing</code> lets an optional database degrade gracefully: when it cannot be reached, the run reports a partial-success exit code instead of failing the whole operation, which keeps CI green when a dependency is not part of the local environment.</p><p>The isolation holds at apply and recovery time, not just at generation: <code className="inline-code">status</code> and <code className="inline-code">history</code> are reported per database, and <code className="inline-code">rollback --all</code> reverses each database's own sequence in its own order, so operating several databases never conflates their histories.</p></div><div><span className="comparison-label">two databases in one config</span><CodeBlock>{`class Primary(DbwardenDatabase):\n    database_name = "primary"\n    default = True\n    database_type = "postgresql"\n    database_url_sync = "postgresql://localhost/main"\n    model_paths = ["app.models.primary"]\n\nclass Analytics(DbwardenDatabase):\n    database_name = "analytics"\n    database_type = "clickhouse"\n    database_url_sync = "clickhouse://localhost:8123/analytics"\n    model_paths = ["app.models.analytics"]`}</CodeBlock><span className="comparison-label">operate them together</span><CodeBlock>{`$ dbwarden migrate --database primary\n$ dbwarden migrate --database analytics\n$ dbwarden status --all`}</CodeBlock></div></div>
    </PageSection>
    <PageSection number="07" label="Optional dependencies" title="Drivers and extras, per backend." doc="https://docs.dbwarden.org/installation/">
      <div className="why-split"><div><p>Core dbwarden installs SQLAlchemy and nothing backend-specific; each driver is an extra you opt into. The extra also pulls the driver's transitive dependencies (for ClickHouse that means aiohttp alongside clickhouse-connect), and combining extras in one command is the normal case for a multi-database repository.</p><p>FastAPI integration has its own extras on the <code className="inline-code">dbwarden-fastapi</code> plugin: <code className="inline-code">[metrics]</code> for prometheus-client, <code className="inline-code">[redis]</code> for the distributed lock, and <code className="inline-code">[clickhouse]</code> for ClickHouse sessions. Those install with <code className="inline-code">uv add "dbwarden-fastapi[metrics]"</code> and are covered on the FastAPI page.</p></div><div><span className="comparison-label">core extras</span><div className="install-list">{extras.map((extra) => <div className="install-row" key={extra.command}><CopyCommand command={extra.command} /><p>{extra.what}</p></div>)}</div></div></div>
    </PageSection>
    <Faq items={[
      { q: 'Which backends have full round-trip?', a: 'PostgreSQL, MySQL, ClickHouse, and SQLite. `Round-trip` means `generate-models` reads a live database back into SQLAlchemy models, and `make-migrations` writes models back to SQL. MariaDB is a migration target with typed metadata, but introspection back into models is not yet supported.' },
      { q: 'What does dev mode translate on SQLite?', a: 'Types SQLite cannot represent are translated: UUID to TEXT, JSON/JSONB to TEXT, BYTEA to BLOB, TIMESTAMPTZ to DATETIME, SERIAL to INTEGER. Unsupported defaults are dropped with a warning, or generation fails under `--strict-translation`. `Round-trip` on a SQLite-native schema needs no translation at all.' },
      { q: 'How does dbwarden handle changes SQLite cannot ALTER?', a: 'The SQLite ALTER TABLE statement covers RENAME TO, RENAME COLUMN, ADD COLUMN, and DROP COLUMN. Any other change, a column type or default, a constraint, STRICT or WITHOUT ROWID, is emitted as a table rebuild: create the new shape, copy the rows, swap. The rebuild is visible in the generated SQL and `check` reports it explicitly.' },
      { q: 'How do I install the driver for another backend?', a: 'PostgreSQL ships by default. Add the matching extra: `uv add "dbwarden[mysql]"`, `uv add "dbwarden[clickhouse]"`, or `uv add "dbwarden[sqlite]"` (the last is empty). Any SQLAlchemy-compatible driver URL also works, for example `mysql+mysqlconnector://`.' },
      { q: 'How do several databases coexist in one repository?', a: 'Each database gets its own migration directory, model set, and versioned history. `model_tables` assigns table ownership when databases share model paths. Operate them with `--database` one at a time or `--all` together; `skip_if_missing` degrades optional databases gracefully.' },
      { q: 'What happens on a lossy engine change in ClickHouse?', a: 'Engine changes go through an explicit recreate policy, and transitions between engine families that would lose data are refused as rollback rather than silently claimed.' },
      { q: 'Do the backend extras overlap with the fastapi extras?', a: 'No. The core extras (`postgres`, `mysql`, `clickhouse`, `sqlite`) install drivers for dbwarden itself. The `dbwarden-fastapi` plugin has its own extras: `[metrics]` for prometheus-client, `[redis]` for the distributed lock, and `[clickhouse]` for ClickHouse sessions.' },
    ]} />
  </PageFrame>
}
