import { PageFrame } from './pages.jsx'

const comparisons = [
  { name: 'Alembic', slug: '/compare/alembic', description: 'The established imperative migration tool for SQLAlchemy.', status: 'Detailed comparison available', href: '/compare/alembic' },
  { name: 'Atlas', description: 'A language-agnostic declarative schema platform with HCL, SQL, and ORM inputs.', status: 'Read the article', href: 'https://blog.emiliano-go.com/works/dbwarden-vs-atlas' },
  { name: 'Django migrations', description: 'The model-driven workflow that inspired dbwarden for non-Django SQLAlchemy stacks.', status: 'Read the article', href: 'https://blog.emiliano-go.com/works/dbwarden-vs-django-migrations' },
]

export function ComparePage({ dark, toggleTheme }) {
  return <PageFrame dark={dark} toggleTheme={toggleTheme} eyebrow="compare" title={<>Choose the tool<br /><em>that fits the team.</em></>} intro="Migration tools make different bets about where schema truth lives, what gets reviewed, and when risk becomes visible. Compare those bets directly.">
    <section className="compare-principle"><div className="section-label">/ the short version</div><div className="compare-principle-grid"><strong>dbwarden is for SQLAlchemy teams that want models as authority and plain SQL as the frozen artifact.</strong><p>That is not automatically better than every alternative. It is a deliberate fit. These comparisons explain the tradeoffs so you can choose without marketing fog.</p></div></section>
    <section className="compare-list"><div className="section-label">/ comparison index</div><div className="compare-cards">{comparisons.map((comparison, index) => <a className="compare-card" href={comparison.href} target={comparison.href.startsWith('http') ? '_blank' : undefined} rel={comparison.href.startsWith('http') ? 'noreferrer' : undefined} key={comparison.name}><div><span>0{index + 1}</span><em>{comparison.status}</em></div><h2>dbwarden<br /><strong>vs {comparison.name}</strong></h2><p>{comparison.description}</p><span className="compare-arrow">↗</span></a>)}</div></section>
    <section className="compare-guidance"><div className="section-label">/ decide by constraint</div><div className="compare-guidance-grid"><div><strong>Choose dbwarden</strong><p>SQLAlchemy is your native ecosystem. You want generated SQL reviewed in pull requests, rollback beside upgrade, application-aware impact analysis, and offline generation from committed state.</p></div><div><strong>Keep looking</strong><p>You need a polyglot schema platform, Django's integrated migration graph, declarative apply for ephemeral environments, or a commercial platform team around the tool.</p></div></div></section>
  </PageFrame>
}

export function AlembicComparisonPage({ dark, toggleTheme }) {
  return <PageFrame dark={dark} toggleTheme={toggleTheme} eyebrow="compare / alembic" title={<>dbwarden<br /><em>vs Alembic.</em></>} intro="Two serious tools for SQLAlchemy. The difference is not quality. It is where each tool puts authority, flexibility, and operational responsibility.">
    <section className="comparison-verdict"><span>the verdict</span><strong>Use dbwarden when you want models to remain the source of truth and migrations to become plain SQL receipts.</strong><p>Use Alembic when you want an established imperative revision system, maximum Python-level control, or the broadest existing SQLAlchemy migration ecosystem.</p></section>
    <ComparisonBlock number="01" title="Where truth lives"><div className="comparison-columns"><div><span className="comparison-label">dbwarden</span><h2>Desired state in models.</h2><p>SQLAlchemy models define what the schema should be. Generated migration files are artifacts of that state, useful for review and deployment but not a second schema to maintain.</p></div><div><span className="comparison-label muted">Alembic</span><h2>Revision chain as authority.</h2><p>Each revision describes the transition from one version to the next. Models and the migration chain are two representations, and keeping them aligned is the team's responsibility.</p></div></div></ComparisonBlock>
    <ComparisonBlock number="02" title="What gets reviewed"><div className="comparison-columns"><div><span className="comparison-label">dbwarden</span><h2>Plain SQL.</h2><p>The file in the pull request contains the SQL that will run, with upgrade and rollback sections together. A DBA or any SQL-capable deployment system can execute it.</p></div><div><span className="comparison-label muted">Alembic</span><h2>Python operations.</h2><p>The revision contains upgrade and downgrade functions. You can inspect rendered SQL, but the artifact itself is Python executed through Alembic's runtime.</p></div></div></ComparisonBlock>
    <ComparisonBlock number="03" title="The escape hatch"><div className="comparison-columns"><div><span className="comparison-label">dbwarden</span><h2>Rollback as contract.</h2><p>Executable rollback is generated with the upgrade. Placeholder rollback is refused by default. Irreversible changes must be declared explicitly and visibly.</p></div><div><span className="comparison-label muted">Alembic</span><h2>Downgrade as responsibility.</h2><p>Every revision can define downgrade, but whether it exists, works, and remains correct is left to the author and the review process.</p></div></div></ComparisonBlock>
    <ComparisonBlock number="04" title="When risk appears"><div className="comparison-columns"><div><span className="comparison-label">dbwarden</span><h2>At generation time.</h2><p>Snapshots make diffs deterministic. Explicit rename flags prevent ambiguous drop-and-create behavior. Impact analysis can inspect application references before destructive changes ship.</p></div><div><span className="comparison-label muted">Alembic</span><h2>At revision review.</h2><p>Autogenerate is useful, but the generated revision is still a candidate Python file that must be checked and corrected by the team.</p></div></div></ComparisonBlock>
    <section className="comparison-table-section"><div className="section-label">/ at a glance</div><table><thead><tr><th>Concern</th><th>dbwarden</th><th>Alembic</th></tr></thead><tbody><tr><th>Source of truth</th><td>SQLAlchemy models</td><td>Revision chain</td></tr><tr><th>Artifact</th><td>Plain SQL</td><td>Python revision</td></tr><tr><th>Rollback</th><td>Generated contract</td><td>Author-defined downgrade</td></tr><tr><th>Offline generation</th><td>Committed model state</td><td>Offline SQL rendering</td></tr><tr><th>Runtime</th><td>SQL runner or any SQL system</td><td>Python and Alembic runtime</td></tr></tbody></table></section>
    <section className="comparison-honesty"><strong>Where Alembic is the better call</strong><p>You need arbitrary Python in migration steps, deep custom revision control, or an existing organization built around Alembic conventions. Alembic is mature, respected, and often exactly right.</p><a className="text-link" href="https://blog.emiliano-go.com/works/dbwarden-vs-alembic" target="_blank" rel="noreferrer">Read the full comparison <span>↗</span></a></section>
  </PageFrame>
}

function ComparisonBlock({ number, title, children }) {
  return <section className="comparison-block"><div className="section-label">/ {number} <span>{title}</span></div>{children}</section>
}
